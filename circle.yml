version: 2
jobs:
  build:
    docker:
      - image: rust
    environment:
      - NIGHTLY: nightly-2017-11-04
    parallelism: 3
    steps:
      - checkout
      - restore_cache:
          keys:
            - cache-
      - run:
          name: Test
          command: scripts/test.sh
      - save_cache:
          key: cache-{{ epoch }}
          paths:
            - /usr/local/cargo
